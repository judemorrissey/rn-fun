import Grid from 'lib/Grid';

/**
 * Represents a grid of numbers. Intended to function similarly to the
 * mathematical construct e.g. has methods for matrix arithmetic.
 */
export default class Matrix extends Grid<number> {
  // TODO:
}
